#version 450
// layout(location = 0) out vec4 outColor;

// Пустой фрагментный шейдер для depth-only рендеринга
void main() {
    // Ничего не выводим, глубина записывается автоматически
    // outColor = vec4(1.0, 0.0, 0.0, 1.0);

}
